version = 1

cloudstream {
    description = "Nonton 29+ platform Drama Pendek China & Korea sub Indo gratis. BiliTV, DramaBox, Melolo, dan banyak lagi."
    language = "id"
    authors = listOf("Makairamei")

    /**
     * Status int as the following:
     * 0: Down
     * 1: Ok
     * 2: Slow
     * 3: Beta only
     * */
    status = 1
    tvTypes = listOf(
        "AsianDrama",
        "TvSeries",
    )

    iconUrl = "https://klikfilm.web.id/favicon.ico"
}
